# Dropbox 옵시디언 → 아침 Gmail

코드와 테스트는 준비되어 있습니다. 실제 계정 연결, 보관함 수정, 예약 및 메일 발송은 아직 하지 않았습니다.

## 1. 노트 준비

동봉한 `오늘의 할 일.md`를 Dropbox 안의 옵시디언 보관함에 복사하세요. 같은 이름의 노트가 이미 있으면 덮어쓰지 말고 기존 노트를 사용하세요. Dropbox 웹에서도 파일이 보이는지 확인합니다.

미완료는 `- [ ]`, 완료는 `- [x]`입니다. 들여쓰기된 체크박스도 읽습니다. 제목이나 분류 형식은 자유이며, 미완료 항목 전체를 노트 순서대로 나열합니다. 날짜와 ⭐는 본문 그대로 표시하며 정렬이나 경고에 사용하지 않습니다. 체크박스 한 줄이 할 일 하나이며 하위 설명은 읽지 않습니다. 코드 블록과 YAML 속성은 제외합니다. 반복 일정이나 Tasks 플러그인 고유 상태는 지원하지 않습니다.

## 2. Apps Script 프로젝트 준비

1. 수신할 본인 Gmail 계정으로 https://script.google.com 에서 새 프로젝트를 만듭니다.
2. `Code.gs` 내용을 편집기의 Code.gs에 복사합니다.
3. 프로젝트 설정에서 '편집기에 appsscript.json 매니페스트 파일 표시'를 켜고, 동봉한 `appsscript.json` 내용을 붙여 넣습니다.
4. 프로젝트 설정 → 스크립트 속성에 아래 항목을 추가합니다. 인증 값은 채팅이나 코드에 적지 마세요. 이 프로젝트는 본인만 편집하도록 유지합니다. 프로젝트 편집자는 스크립트 속성을 읽을 수 있습니다.

| 속성 | 값 |
|---|---|
| `RECIPIENT_EMAIL` | 본인의 Gmail 주소 한 개 |
| `DROPBOX_NOTE_PATH` | Dropbox 루트 기준 경로. 예: `/Obsidian/내 보관함/오늘의 할 일.md` |
| `DROPBOX_APP_KEY` | 아래에서 만든 Dropbox 앱의 App key |
| `DROPBOX_APP_SECRET` | 해당 앱의 App secret |

Windows의 `C:\Users\...` 경로를 넣지 않습니다. 이 버전은 개인 Dropbox 사용자 계정의 기본 루트를 대상으로 합니다. 팀 공간의 별도 namespace는 지원하지 않습니다.

## 3. Dropbox 인증 설정

1. https://www.dropbox.com/developers/apps 에서 본인 전용 앱을 만듭니다. 기존 보관함을 읽기 위해 Scoped access / Full Dropbox를 선택합니다.
2. Permissions에서 `files.content.read`를 활성화하고 저장합니다. 쓰기·공유·팀 권한은 필요하지 않습니다.
3. App key와 App secret을 위 스크립트 속성에 저장합니다.
4. Apps Script에서 `showAuthorizationUrl`을 실행합니다. 실행 로그의 Dropbox 주소를 열어 본인 계정으로 허용합니다. redirect URI는 사용하지 않습니다.
5. Dropbox가 표시한 일회용 코드를 스크립트 속성 `DROPBOX_AUTH_CODE`에 저장합니다.
6. `exchangeAuthorizationCode`를 실행합니다. Google 권한 화면이 나오면 앱이 본인 프로젝트인지 확인하고 필요한 권한을 허용합니다. 갱신 토큰은 `DROPBOX_REFRESH_TOKEN`에 저장되고 일회용 코드는 삭제됩니다. 토큰은 로그에 출력되지 않습니다.

Full Dropbox의 읽기 권한 자체는 계정 전체를 대상으로 합니다. 코드는 지정한 노트 하나만 다운로드하며 파일 목록 조회나 다른 파일 읽기를 하지 않습니다. 기존 위치를 유지하는 대신 이 권한 범위를 사용하는 구조입니다.

## 4. 미리보기와 활성화

1. `previewDigest`를 실행합니다. Dropbox 갱신 토큰으로 새 액세스 토큰을 받고 노트를 읽어 메일 내용을 실행 로그에 표시합니다. 실제 메일은 보내지 않습니다. 로그에는 할 일 본문이 들어갑니다.
2. 미완료 전체 목록의 순서와 노트 열기 링크를 확인합니다. 링크는 로그인한 Dropbox 웹에서 파일을 여는 용도이며 공개 공유 링크를 만들지 않습니다.
3. 자동화를 시작하기로 했을 때만 `installSchedule`을 실행합니다. 5분 간격 트리거를 만들며 즉시 발송하지 않습니다. 07:00 이후에 설치하면 그날 다음 실행에서 보낼 수 있습니다.
4. 다음 아침 Gmail에서 확인합니다. 중지할 때는 `removeSchedule`을 실행합니다.

Google 권한 목록에는 외부 API 요청, 메일 보내기, 트리거 관리가 포함됩니다. Gmail 받은편지함 읽기와 Google Drive 권한은 요구하지 않습니다. 웹 앱 배포나 Dropbox 플러그인 설치는 필요하지 않습니다.

## 동작과 한계

- 한국 시간 매일 07:00 이후 첫 실행에서 주말 포함 하루 한 번 시도합니다. 목표는 07:00~07:05이며 Google 실행 지연으로 늦어질 수 있습니다. 늦게 복구되면 당일 07:00 이후 언제든 발송할 수 있고, 지난 날짜의 메일은 소급하지 않습니다.
- 모든 미완료 체크박스를 노트 순서대로 ‘남은 할 일’ 목록 하나에 표시합니다. 개수 제한, 분류, 우선순위와 선정 이유는 없습니다.
- 컴퓨터를 꺼도 Google에서 실행합니다. 단, Dropbox에 올라가지 않은 변경은 읽을 수 없습니다. 메일에는 Dropbox 서버의 마지막 수정 시각을 표시하며 동기화 정상 여부는 추정하지 않습니다.
- 읽기 또는 인증 실패 시 그날 한 번 확인 실패 메일을 보내며 다음날 다시 시도합니다. 기본 설정 오류나 Google 할당량 부족은 Apps Script 실행 실패로 남습니다.
- 동시 실행 잠금과 발송 전 날짜 기록으로 중복 시도를 막습니다. 발송 중 오류가 나거나 실행이 중단되면 그날 메일이 누락될 수 있습니다. MailApp에는 정확히 한 번 배달을 보장하는 기능이 없어 중복 방지를 우선했습니다.
- 스크립트 속성 `LAST_STATUS`가 `delivery_uncertain` 또는 오래된 `sending`이면 Gmail 수신 여부를 먼저 확인합니다. 누락이 확인되어 당일 재시도가 필요할 때만 `LAST_ATTEMPT_DATE`를 삭제합니다. 예약이 켜져 있으면 다음 실행에서 발송을 다시 시도합니다.

## 검증

로컬에서 `node test.cjs`를 실행하면 외부 연결 없이 모의 테스트를 수행합니다. 파싱, 전체 목록과 원문 순서 유지, 한국어 경로, 인증 갱신 요청, 읽기 실패, 중복 방지, 발송 오류, 미리보기, 예약 설치/삭제를 검사합니다.

실계정 연결 후 확인해야 하는 항목은 OAuth 승인·갱신 성공, 실제 Dropbox 다운로드, 링크 열기, Gmail 수신, 컴퓨터를 끈 다음날 예약 수신입니다. 이 항목들은 현재 실행하지 않았습니다.

## 공식 참고 자료

- Dropbox OAuth와 오프라인 갱신: https://developers.dropbox.com/oauth-guide
- Dropbox 파일 접근: https://developers.dropbox.com/dbx-file-access-guide
- Google 시간 트리거: https://developers.google.com/apps-script/reference/script/clock-trigger-builder
- Google MailApp: https://developers.google.com/apps-script/reference/mail/mail-app
- Google 실행 잠금: https://developers.google.com/apps-script/reference/lock/lock-service

## 기존 버전 업데이트

Apps Script의 `Code.gs` 전체를 최신 내용으로 교체하고 저장하세요. `previewDigest`로 새 목록을 확인할 수 있습니다. 기존 인증 속성, 발송 이력, 예약 트리거는 그대로 유지합니다. 이미 예약했다면 `installSchedule`을 다시 실행할 필요가 없습니다.
